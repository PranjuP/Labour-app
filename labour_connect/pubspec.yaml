name: labour_connect
description: Labour Connect — a platform connecting companies with labourers.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1
  cloud_firestore: ^6.4.1
  shared_preferences: ^2.0.11
  provider: ^6.0.3
  url_launcher: ^6.1.5
  animated_splash_screen: ^1.3.0
  font_awesome_flutter: ^11.0.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
